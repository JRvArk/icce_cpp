Convert verbal variable and function descriptions to declarations.

1) Declare a variable ptr points to rows of 8 doubles.

double (*ptr)[8];

2) declare a variable ptr pointing to an array holding the addresses of
    series of doubles.

double **ptr;

3) declare a pointer ptr that may point to arrays of immutable pointers to 
    arrays of mutable pointers to immutable std::string objects.

std::string const * * const * ptr;

4) declare a function fun expecting no parameters, returning a matrix of 6 x 6 
    std::string objects.

std::string **fun();

5) Declare a function fun as in the previous item, now using StringMat;

