#include <iostream>
#include <string>

using namespace std;

int main()
{
    string inputText;
    while (getline(cin, inputText));
        if (inputText.length() <= 72)
            cout << inputText << '\n';
        else if (inputText.length() > 72)
        {
            
        }
        
}