#include <iostream>
#include <string>

using namespace std;

struct ArgumentStruct { // HB: L
    bool charArg = false;
    bool wordArg = false;
    bool lineArg = false;
};

ArgumentStruct checkMode(string arg);

size_t charCount();

size_t wordCount();

size_t lineCount();
