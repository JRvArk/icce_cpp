#include <iostream>
#include <string>

using namespace std;


enum Mode{
    CHARCOUNT,
    WORDCOUNT,
    LINECOUNT,
    NONE
};

Mode checkMode(string arg);

size_t charCount();

size_t wordCount();

size_t lineCount();
